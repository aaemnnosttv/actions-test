name: CI

on:
  push:
    branches: master
  pull_request:
    types: [opened, synchronize, reopened, closed]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Dump Context
        run: echo '${{ toJSON( github ) }}'
